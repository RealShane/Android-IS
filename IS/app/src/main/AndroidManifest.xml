<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.is.shane">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STAT" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UndoneActivity"></activity>
        <activity android:name=".banner.pagerOnClickListener" />
        <activity android:name=".banner.LoopViewAdapter" />
        <activity android:name=".secondclass.SignSucceedActivity" />
        <activity android:name=".secondclass.SingleCourseActivity" />
        <activity android:name=".bean.Course" />
        <activity
            android:name=".SecondClassActivity"
            android:label="第二课堂" />
        <activity android:name=".DrawerTabFragment" />
        <activity
            android:name=".MainActivity"
            android:label="综合系统">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>